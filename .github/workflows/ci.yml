name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

permissions:
  contents: read
  pull-requests: write

# Configuration: Update Go versions below
# When changing versions, update in 3 places: test-postgres, test-mysql, test-sqlite
# MAIN_GO_VERSION: Version used for coverage upload (currently: '1.25')
# GO_VERSIONS: JSON array of versions to test (currently: '["1.25"]')

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
          cache: true
      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.11.2
          args: --timeout=5m

  test-postgres:
    name: Integration tests [PostgreSQL]
    uses: ./.github/workflows/integration-test.yml
    permissions:
      contents: write
      pull-requests: write
    with:
      main_go_version: '1.25'
      go_versions: '["1.25"]'
      db_driver: 'postgres'
      db_dsn: 'postgres://postgres:postgres@localhost:5432/postgres?sslmode=disable'

  test-mysql:
    name: Integration tests [MySQL]
    uses: ./.github/workflows/integration-test.yml
    permissions:
      contents: write
      pull-requests: write
    with:
      main_go_version: '1.25'
      go_versions: '["1.25"]'
      db_driver: 'mysql'
      db_dsn: 'root:root@tcp(localhost:3306)/goque?parseTime=true&loc=UTC'

  test-sqlite:
    name: Integration tests [SQLite]
    uses: ./.github/workflows/integration-test.yml
    permissions:
      contents: write
      pull-requests: write
    with:
      main_go_version: '1.25'
      go_versions: '["1.25"]'
      db_driver: 'sqlite3'
      db_dsn: 'goque_test.db'

  report-coverage:
    name: Report Coverage
    uses: ./.github/workflows/coverage.yml
    needs: [test-postgres, test-mysql, test-sqlite]
    permissions:
      contents: write
      pull-requests: write
